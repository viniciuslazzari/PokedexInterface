import styled from 'styled-components'

const CardLineStyle = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    margin-left: auto;
    margin-right: auto;
`;

export default CardLineStyle